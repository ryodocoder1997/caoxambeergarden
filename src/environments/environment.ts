// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCmK8M9vzoSr22S7jcNFHWOj1Z3-NmuUKc",
    authDomain: "caoxambeerclub.firebaseapp.com",
    databaseURL: "https://caoxambeerclub.firebaseio.com",
    projectId: "caoxambeerclub",
    storageBucket: "caoxambeerclub.appspot.com",
    messagingSenderId: "226274558328",
    appId: "1:226274558328:web:0d47b6f7f0a3011d5e088d",
    measurementId: "G-57BHSW2JP4"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
